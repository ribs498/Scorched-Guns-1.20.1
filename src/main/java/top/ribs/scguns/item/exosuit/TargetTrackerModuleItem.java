package top.ribs.scguns.item.exosuit;

public class TargetTrackerModuleItem extends EnergyUpgradeItem {
    public TargetTrackerModuleItem(Properties properties) {
        super(properties, EnergyConsumptionType.PER_TICK);
    }
}